mutation ForgotPassword($emailId: String!) {
  forgotPassword(emailId: $emailId, input: {})
    @rest(
      type: "ForgotPassword"
      path: "/auth/{args.emailId}/resetPassword"
      method: "POST"
    ) {
    NoResponse
  }
}
