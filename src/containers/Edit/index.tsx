import React from "react";

const edit: React.CSSProperties = {
  textAlign:"center",
  paddingTop:"100px"
};

const Edit: React.FC = () => {
  return <div style={edit}>Edit</div>;
};

export default Edit;