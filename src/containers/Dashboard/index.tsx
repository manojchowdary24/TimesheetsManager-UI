import React from "react";

const dashboard: React.CSSProperties = {
  textAlign:"center",
  paddingTop:"100px"
};

const Dashboard: React.FC = () => {
  return <div style={dashboard}>Dashboard</div>;
};

export default Dashboard;