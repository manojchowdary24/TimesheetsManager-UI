import React from "react";

const view: React.CSSProperties = {
  textAlign:"center",
  paddingTop:"100px"
};

const View: React.FC = () => {
  return <div style={view}>view</div>;
};

export default View;