import React from "react";

const settings: React.CSSProperties = {
  textAlign:"center",
  paddingTop:"100px"
};

const Settings: React.FC = () => {
  return <div style={settings}>Settings</div>;
};

export default Settings;