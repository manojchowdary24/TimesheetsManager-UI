import React from "react";

const add: React.CSSProperties = {
  textAlign:"center",
  paddingTop:"100px"
};

const Add: React.FC = () => {
  return <div style={add}>Add</div>;
};

export default Add;