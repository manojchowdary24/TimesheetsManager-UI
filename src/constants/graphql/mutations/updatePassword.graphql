mutation UpdatePassword($email: String!, $input: UpdatePasswrodInput!) {
  updatePassword(email: $email, input: $input)
    @rest(
      type: "UpdatePassword"
      path: "/auth/{args.email}/updatePassword"
      method: "POST"
    ) {
    NoResponse
  }
}
